# EasyInput V0.1（EXE 免安装版）

> 打包日期：2026-09-18 ｜ 版本号：**V0.1** ｜ 形态：**目录版（免安装）**

## 怎么用

1. 把**整个 `V0.1` 文件夹**拷到任意位置（U 盘/桌面都行，**别只拷 exe**，它要读同目录的 `_internal`）。
2. 双击 **`EasyInput.exe`**。
3. 首次运行会在 **EXE 同目录**生成/读取这些（绿色版可随身带）：

```
V0.1/
├── EasyInput.exe
├── _internal/            ← 程序本体（界面、依赖库、UIA 驱动 DLL）
├── config.json           ← 你的配置（首次运行自动生成；key 只在本机）
├── macros.json           ← 宏（出厂样例，可改）
├── models/               ← 本地语音识别模型（首次用"本地 whisper"时自动下载，几百 MB）
└── last_recording.wav    ← 最近一次录音（排查用）
```

## 环境要求

| 项 | 要求 | 说明 |
|---|---|---|
| 系统 | Windows 10/11 x64 | 用了 WebView2、SendInput、UIA |
| **WebView2 Runtime** | **必须** | 界面是网页渲染的；没有会弹提示并给出安装命令<br>`winget install Microsoft.EdgeWebView2Runtime`（Win11 一般自带） |
| 板子 | 可选 | 不连板子也能用转录/宏/划词编辑，只是没有按键触发 |

## V0.1 有什么（相对源码版的功能对照）

- **转录**：板子录音 → 本地 whisper / 在线 STT → 打字或进历史
- **划词 AI 编辑**：选中一段 → 按住录音键说"翻译成英文 / 帮我写得专业一点" → **替换选中内容**（Ctrl+Z 可还原）
- **语音提问键**：不选任何东西，按住键直接问（"纳斯达克现在多少点"）→ 答案打字出来
- **软件自己联网**：免 Key（搜狗 → 360 → Bing 自动换源 + 抓网页正文 + 行情/天气实时值）
- **宏 / 按键映射 / 灯效 / 播放器 / 模块**（与源码版一致）
- **大模型配置**：设置页 → STT 引擎 → **文本模型**（划词编辑改文用）

## 已知边界（不是 bug）

| 边界 | 说明 |
|---|---|
| **本地 STT 首次要下模型** | 几百 MB，下到 `models/`；网络不好会慢（也可改用在线 STT） |
| **UIA 抓选区在某些应用无效** | 终端类/部分自绘控件抓不到 → 自动退 Ctrl+C 方式；仍不行则回落成普通转录 |
| **回车注入到聊天软件** | 微信/QQ 里多行输出可能被"回车=发送"影响（可 `setAiEdit(inject='paste')`） |
| **思考型模型** | 如 `deepseek-flash`：改文时若输出额度不足会提示"只回了思考过程" → 设置 `aiedit.max_out=8192` |
| **单实例** | 重复双击会提示"已在运行"（防两个实例抢串口） |
| **首次启动稍慢** | 目录版约 2~5 秒（解压依赖 + 起 WebView2） |

## 排查

| 现象 | 看哪里 |
|---|---|
| 启动闪一下就没了 | EXE 同目录 `easyinput_exe_error.log`（入口会自动写异常栈） |
| 界面全白 | 缺 WebView2 Runtime（会弹提示） |
| 界面显示版本不对 | 主页右上角 `V0.1`；源码运行会显示"（源码）" |
| 日志 | 界面底部日志条 + `app_log.txt`（EXE 同目录） |

## 怎么重新打包（源码侧）

```powershell
cd "d:\Easyinput project\软件平台\app"
..\venv\Scripts\python.exe -m PyInstaller easyinput.spec --noconfirm
# 产物：app\dist\EasyInput\  → 拷到 发布\Vx.y\
```

打包配置见 `app/easyinput.spec`（排除重型包 + UIA DLL + 界面资源），入口是 `app/exe_main.py`（假 tkinter + 版本号 + 异常兜底）。
