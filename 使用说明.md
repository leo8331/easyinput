# EasyInput V0.1 · 使用说明

> 免安装版 · Windows 10/11 x64 · 2026-09-18

## 一、怎么用（3 步）

1. **解压**（重要！）：右键下载的 zip → **全部解压**到任意位置（桌面 / D 盘 / U 盘都行）
   > ⚠️ **不要直接在压缩包里双击运行** —— 那样设置不会保存（见第五节）
2. 双击文件夹里的 **`EasyInput.exe`**
3. 按界面提示配一次识别引擎：
   - 想**离线免费** → 设置 → STT 引擎 → 选「本地 whisper」（首次会自动下模型）
   - 想用**云端** → 选「OpenAI 兼容」，填接口地址与 Key

> 💡 **整个文件夹要一起拷**，别只拷 exe —— 它需要旁边的 `_internal` 文件夹。

## 二、前置条件

| 项 | 要求 |
|---|---|
| 系统 | Windows 10 / 11 **64 位** |
| **WebView2 Runtime** | **必需**（界面靠它渲染）。Win11 一般自带；缺了软件会弹提示，照提示装即可 |
| 硬件（可选） | EasyInput V2.0 开发板 —— **不接也能用**（转录、划词编辑、宏都不依赖硬件），只是没有实体按键触发 |

## 三、能用它做什么

| 场景 | 怎么做 |
|---|---|
| **说话打字** | 按住板子录音键说话 → 文字直接出现在光标处（可离线） |
| **改写文字** | 先**选中一段** → 按住录音键说"翻译成英文 / 帮我写得专业一点" → 选中的字被替换（`Ctrl+Z` 可还原） |
| **问问题** | 按住**提问键**直接问"纳斯达克现在多少点" → 答案打进光标处（软件自己联网查，不用配 Key） |
| **快捷操作** | 宏：一键打开程序/网址、输入常用语、组合键 |
| **氛围** | 灯效（频谱/呼吸/场景色）、把音乐传到板子喇叭播放 |

**默认按键**（可在「按键映射」页改）：

| 键 | 作用 |
|---|---|
| `S1` | 录音（转录 / 划词编辑） |
| `S7` | 提问键（不选文字直接问，需在设置里绑定） |
| 旋钮 | 上下滚动 / 按压切换方向 |

## 四、离线识别（本地 whisper）

**会自动下载**，不用手动准备。国内网络下默认走镜像，速度正常。

- **方式 A（推荐）**：设置 → STT 引擎 → 选「本地 whisper」→ 第一次说话时自动下载（约 460 MB）→ 之后**完全离线**
- **方式 B（无网/网慢）**：把 Release 页面的 `models-faster-whisper-small.zip` 解压到 **exe 同级目录**（得到 `models/` 文件夹）→ 重启软件即可
- **方式 C**：不用本地识别 —— 选「OpenAI 兼容」填云端接口

> 模型大小可选：`tiny`（最快）/ `base` / `small`（默认）/ `medium`（最准最慢）

## 五、设置会保留吗？**会**

配置写在 **exe 同目录**的 `config.json`，关掉软件再打开仍在，**整个文件夹拷到别的电脑也带着配置**。

| 你把它放在哪 | 配置存在哪 | 会保留吗 |
|---|---|---|
| 桌面 / D 盘 / U 盘（正常解压） | 同目录 `config.json` | ✅ 保留，可随身带 |
| `C:\Program Files` 等受保护位置 | 自动改存 `%APPDATA%\EasyInput\` | ✅ 保留 |
| **直接在压缩包里双击运行** | Windows 临时目录 | ❌ **关掉就丢** |

> 所以：**先解压，再运行**。这是唯一会丢设置的情况。

运行后文件夹里会自动多出这些东西（删掉即恢复默认）：

```
config.json          你的设置（含 API Key —— 只存本机，不会上传）
macros.json          宏配置
models/              本地识别模型
last_recording.wav   最近一次录音（排查用）
app_log.txt          运行日志
```

## 六、遇到问题先看这里

| 现象 | 怎么办 |
|---|---|
| 双击没反应 / 闪一下就没了 | 同目录 `easyinput_exe_error.log` 里有原因；多半是缺 WebView2 Runtime |
| 界面一片空白 | 缺 WebView2 Runtime（弹窗里有安装命令） |
| 提示"已在运行" | 防重复打开的保护（避免两个实例抢硬件）—— 看任务栏 |
| 本地模型下不动 | 改用**方式 B**（解压附带的 zip）；或换云端识别 |
| 选中文字后说指令，没改写 | 那个软件可能不支持读取选区（终端类常见）→ 这时会当作普通转录，属正常回落 |
| 在微信/QQ 里多行内容被直接发出 | 回车被当成发送键 —— 可在设置里把回填方式改成"粘贴" |
| 识别出文字但没落进输入框 | 杀毒软件/权限拦截 —— 把 `EasyInput.exe` 加进信任列表；以管理员身份打开的窗口无法注入 |
| 首次启动慢 2~5 秒 | 正常（要解压依赖 + 启动渲染引擎） |

## 七、常见问题

**Q：一定要买硬件才能用吗？**
不用。软件可以单独使用（转录、划词编辑、宏都可用），硬件只是提供"按住说话"的实体按键。

**Q：要联网吗？**
识别可以完全离线（本地 whisper）；只有"提问键"和划词编辑需要联网（调用大模型/搜索）。

**Q：API Key 安全吗？**
只存在本机 `config.json`，不会上传；软件界面只显示掩码。

**Q：能在 macOS 上用吗？**
本版本仅 Windows。上游官方 App 支持 macOS，见 [easyinput.pro](https://easyinput.pro/)。

---

> 本软件基于 [EasyInput V2.0](https://easyinput.pro/)（上游产品）二次开发，仅供学习与非商业使用。
> 它会模拟键盘输入并读取剪贴板/选区，用于把识别结果打进当前窗口 —— 请自行确认在网银、密码框等敏感场景中的使用风险。
